#include<iostream>
using namespace std;
class Node {
public:
    int data;
    Node *next;
    Node(int value)
    {
        data = value;
        next=  NULL;
    }
};
/*int main(){
Node *HEAD;
HEAD = new Node(28);   // new keyword returns the adress of Node
    cout<<HEAD->data<<endl; // object.() is used for objects
    //but here head is a pointer and to acess what it stores we use ->
    cout<<HEAD->next<<endl;
}
*/
int main() {


}
